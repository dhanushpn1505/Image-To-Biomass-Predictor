# Image-To-Biomass-Predictor
Deep learning project that predicts crop biomass from RGB images using a pretrained ResNet18 Convolutional Neural Network (CNN). The model is fine-tuned for regression using PyTorch and evaluated with RMSE.
